<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Brushes.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Continuous rotation used by the syncing spinner overlay -->
    <Storyboard x:Key="Storyboard.SpinForever" RepeatBehavior="Forever">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                         From="0" To="360" Duration="0:0:1.1"/>
    </Storyboard>

    <!-- Pulsing opacity used by "syncing" text / dots -->
    <Storyboard x:Key="Storyboard.PulseForever" RepeatBehavior="Forever" AutoReverse="True">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0.35" To="1.0" Duration="0:0:0.9"/>
    </Storyboard>

    <!-- Ease used across most short UI transitions -->
    <CubicEase x:Key="Ease.Standard" EasingMode="EaseOut"/>

    <!-- Fade-in used for newly appeared elements (toasts, wizard steps) -->
    <Storyboard x:Key="Storyboard.FadeIn">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.22">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="Storyboard.FadeOut">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="1" To="0" Duration="0:0:0.18"/>
    </Storyboard>

    <!-- Brief scale+glow pulse played while a drag hovers over a Source card or a LogType chip
         (see Common/DragDropBehavior.cs's IsDragOver attached property, toggled on a
         DataTrigger in SourceCardTemplate.xaml/LogTypeChip.xaml). RepeatBehavior=Forever so it
         keeps gently pulsing for as long as the drag lingers, rather than firing once and
         stopping while the user is still deciding whether to drop. Targets a RenderTransform on
         the element the trigger is set on - callers must give that element a
         ScaleTransform RenderTransformOrigin="0.5,0.5" (see SourceCardTemplate.xaml's outer
         Border and LogTypeChip's ChipBorder). -->
    <Storyboard x:Key="Storyboard.DragPulse" RepeatBehavior="Forever" AutoReverse="True">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                         To="1.035" Duration="0:0:0.5">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseInOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                         To="1.035" Duration="0:0:0.5">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseInOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

</ResourceDictionary>
